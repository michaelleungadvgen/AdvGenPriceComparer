<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="AdvGenPriceComparer.Desktop.WinUI.Views.AddPlaceView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollViewer MaxHeight="500" VerticalScrollBarVisibility="Auto">
        <StackPanel Spacing="16" Margin="8">
            <!-- Header -->
            <TextBlock Text="Add New Store/Supermarket" 
                      FontSize="18" FontWeight="SemiBold"
                      Margin="0,0,0,8"/>
            
            <TextBlock Text="Add a new supermarket or store location to track grocery prices." 
                      TextWrapping="Wrap" 
                      Foreground="{StaticResource TextSecondaryBrush}"
                      Margin="0,0,0,12"/>

            <!-- Basic Store Information -->
            <Border Background="{StaticResource CardBackgroundBrush}" 
                    CornerRadius="8" Padding="16">
                <StackPanel Spacing="12">
                    <TextBlock Text="Store Information" FontWeight="SemiBold" FontSize="14"/>
                    
                    <!-- Store Name (Required) -->
                    <StackPanel>
                        <TextBlock Text="Store Name *" FontWeight="Medium"/>
                        <TextBox Text="{Binding StoreName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                PlaceholderText="e.g., Coles Chatswood, Woolworths Town Hall"/>
                    </StackPanel>

                    <!-- Chain (Required) -->
                    <StackPanel>
                        <TextBlock Text="Chain *" FontWeight="Medium"/>
                        <ComboBox ItemsSource="{Binding Chains}" 
                                 SelectedItem="{Binding Chain, Mode=TwoWay}"
                                 PlaceholderText="Select or enter chain"
                                 IsEditable="True"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Location Information -->
            <Border Background="{StaticResource CardBackgroundBrush}" 
                    CornerRadius="8" Padding="16">
                <StackPanel Spacing="12">
                    <TextBlock Text="Location" FontWeight="SemiBold" FontSize="14"/>
                    
                    <!-- Address -->
                    <StackPanel>
                        <TextBlock Text="Street Address" FontWeight="Medium"/>
                        <TextBox Text="{Binding Address, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                PlaceholderText="e.g., 123 Main Street"
                                TextWrapping="Wrap"
                                AcceptsReturn="True"
                                Height="60"/>
                    </StackPanel>

                    <!-- Suburb (Required) -->
                    <StackPanel>
                        <TextBlock Text="Suburb *" FontWeight="Medium"/>
                        <TextBox Text="{Binding Suburb, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                PlaceholderText="e.g., Chatswood, Sydney"/>
                    </StackPanel>

                    <!-- State and Postcode -->
                    <Grid ColumnDefinitions="*,12,150">
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="State *" FontWeight="Medium"/>
                            <ComboBox ItemsSource="{Binding States}" 
                                     SelectedItem="{Binding State, Mode=TwoWay}"
                                     PlaceholderText="Select state"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Postcode" FontWeight="Medium"/>
                            <TextBox Text="{Binding Postcode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                    PlaceholderText="e.g., 2067"/>
                            <TextBlock Text="{Binding PostcodeError, Mode=OneWay}" 
                                      Foreground="Red" FontSize="12" 
                                      Visibility="{Binding HasPostcodeError, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Contact Information -->
            <Border Background="{StaticResource CardBackgroundBrush}" 
                    CornerRadius="8" Padding="16">
                <StackPanel Spacing="12">
                    <TextBlock Text="Contact Information (Optional)" FontWeight="SemiBold" FontSize="14"/>
                    
                    <!-- Phone -->
                    <StackPanel>
                        <TextBlock Text="Phone Number" FontWeight="Medium"/>
                        <TextBox Text="{Binding Phone, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                PlaceholderText="e.g., (02) 9876 5432"/>
                        <TextBlock Text="{Binding PhoneError, Mode=OneWay}" 
                                  Foreground="Red" FontSize="12" 
                                  Visibility="{Binding HasPhoneError, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                    </StackPanel>

                    <!-- Website -->
                    <StackPanel>
                        <TextBlock Text="Website" FontWeight="Medium"/>
                        <TextBox Text="{Binding Website, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                PlaceholderText="https://www.example.com"/>
                        <TextBlock Text="{Binding WebsiteError, Mode=OneWay}" 
                                  Foreground="Red" FontSize="12" 
                                  Visibility="{Binding HasWebsiteError, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Store Features -->
            <Border Background="{StaticResource CardBackgroundBrush}" 
                    CornerRadius="8" Padding="16">
                <StackPanel Spacing="12">
                    <TextBlock Text="Store Features (Optional)" FontWeight="SemiBold" FontSize="14"/>
                    
                    <!-- Operating Hours -->
                    <StackPanel>
                        <TextBlock Text="Operating Hours" FontWeight="Medium"/>
                        <TextBox Text="{Binding OperatingHours, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                PlaceholderText="e.g., Mon-Fri: 6am-10pm, Sat-Sun: 7am-9pm"/>
                    </StackPanel>

                    <!-- Services -->
                    <StackPanel>
                        <TextBlock Text="Services Available" FontWeight="Medium"/>
                        <ItemsControl ItemsSource="{Binding Services}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" Spacing="8"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox Content="{Binding DisplayName}" 
                                             IsChecked="{Binding IsSelected, Mode=TwoWay}"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Location Coordinates (Advanced) -->
            <Expander Header="Advanced Location (GPS Coordinates)" IsExpanded="False">
                <Border Background="{StaticResource CardBackgroundBrush}" 
                        CornerRadius="8" Padding="16" Margin="0,8,0,0">
                    <StackPanel Spacing="12">
                        <TextBlock Text="Enter GPS coordinates for precise location mapping" 
                                  Foreground="{StaticResource TextSecondaryBrush}"/>
                        
                        <Grid ColumnDefinitions="*,12,*">
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Latitude" FontWeight="Medium"/>
                                <TextBox Text="{Binding Latitude, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                        PlaceholderText="e.g., -33.7969"/>
                                <TextBlock Text="{Binding LatitudeError, Mode=OneWay}" 
                                          Foreground="Red" FontSize="12" 
                                          Visibility="{Binding HasLatitudeError, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Longitude" FontWeight="Medium"/>
                                <TextBox Text="{Binding Longitude, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                        PlaceholderText="e.g., 151.1316"/>
                                <TextBlock Text="{Binding LongitudeError, Mode=OneWay}" 
                                          Foreground="Red" FontSize="12" 
                                          Visibility="{Binding HasLongitudeError, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>
            </Expander>

            <!-- Validation Summary -->
            <Border Background="#FFEBEE" 
                   BorderBrush="Red" 
                   BorderThickness="1"
                   CornerRadius="4" 
                   Padding="12"
                   Visibility="{Binding ValidationErrors, Mode=OneWay, Converter={StaticResource StringToVisibilityConverter}}">
                <StackPanel>
                    <TextBlock Text="Please fix the following errors:" 
                              FontWeight="SemiBold" 
                              Foreground="Red"/>
                    <TextBlock Text="{Binding ValidationErrors, Mode=OneWay}" 
                              Foreground="Red" 
                              TextWrapping="Wrap"
                              Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>