<Window x:Class="AdvGenPriceComparer.WPF.Chat.PriceChatWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:models="clr-namespace:AdvGenPriceComparer.WPF.Chat.Models"
        mc:Ignorable="d"
        Title="AI Price Assistant" 
        Height="700" 
        Width="600"
        MinHeight="500"
        MinWidth="450"
        WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <!-- Message Bubble Style for User -->
        <Style x:Key="UserMessageStyle" TargetType="Border">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="CornerRadius" Value="15,15,2,15"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Margin" Value="50,5,10,5"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="MaxWidth" Value="400"/>
        </Style>

        <!-- Message Bubble Style for Assistant -->
        <Style x:Key="AssistantMessageStyle" TargetType="Border">
            <Setter Property="Background" Value="#F0F0F0"/>
            <Setter Property="CornerRadius" Value="15,15,15,2"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Margin" Value="10,5,50,5"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="MaxWidth" Value="400"/>
        </Style>

        <!-- Error Message Style -->
        <Style x:Key="ErrorMessageStyle" TargetType="Border">
            <Setter Property="Background" Value="#FFEBEE"/>
            <Setter Property="BorderBrush" Value="#EF5350"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="15,15,15,2"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Margin" Value="10,5,50,5"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="MaxWidth" Value="400"/>
        </Style>

        <!-- Thinking Indicator Style -->
        <Style x:Key="ThinkingMessageStyle" TargetType="Border">
            <Setter Property="Background" Value="#E3F2FD"/>
            <Setter Property="CornerRadius" Value="15,15,15,2"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Margin" Value="10,5,50,5"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>

        <!-- Suggestion Button Style -->
        <Style x:Key="SuggestionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="#0078D4"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#0078D4"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="15">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E3F2FD"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#0078D4" Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="🤖 AI Price Assistant" 
                               FontSize="20" 
                               FontWeight="Bold" 
                               Foreground="White"/>
                    <TextBlock Text="Ask me about prices, deals, products, or stores" 
                               FontSize="12" 
                               Foreground="#B3D7F7"
                               Margin="0,3,0,0"/>
                </StackPanel>

                <Button Grid.Column="1" 
                        Content="✕" 
                        AutomationProperties.Name="Close"
                        Width="30" 
                        Height="30"
                        Background="Transparent"
                        BorderThickness="0"
                        Foreground="White"
                        FontSize="16"
                        Click="CloseButton_Click"/>
            </Grid>
        </Border>

        <!-- Status Bar -->
        <Border Grid.Row="1" 
                Background="{Binding IsOllamaAvailable, Converter={StaticResource BooleanToBrushConverter}}"
                Padding="10,5">
            <TextBlock Text="{Binding StatusMessage}" 
                       FontSize="11"
                       HorizontalAlignment="Center"
                       Foreground="{Binding IsOllamaAvailable, Converter={StaticResource BooleanToForegroundConverter}}"/>
        </Border>

        <!-- Chat Messages -->
        <ScrollViewer Grid.Row="2" 
                      x:Name="MessagesScrollViewer"
                      VerticalScrollBarVisibility="Auto"
                      Margin="0,10">
            <ItemsControl ItemsSource="{Binding Messages}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:ChatMessage}">
                        <Grid>
                            <!-- User Message -->
                            <Border Style="{StaticResource UserMessageStyle}"
                                    Visibility="{Binding Role, Converter={StaticResource MessageRoleToVisibilityConverter}, ConverterParameter=User}">
                                <TextBlock Text="{Binding Content}" 
                                           Foreground="White"
                                           TextWrapping="Wrap"/>
                            </Border>

                            <!-- Assistant Message -->
                            <Border Style="{StaticResource AssistantMessageStyle}"
                                    Visibility="{Binding Role, Converter={StaticResource MessageRoleToVisibilityConverter}, ConverterParameter=Assistant}">
                                <StackPanel>
                                    <TextBlock Text="{Binding Content}" 
                                               Foreground="#333"
                                               TextWrapping="Wrap"/>

                                    <!-- Attached Items -->
                                    <ItemsControl ItemsSource="{Binding AttachedItems}" 
                                                  Margin="0,10,0,0"
                                                  Visibility="{Binding AttachedItems.Count, Converter={StaticResource CountToVisibilityConverter}}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="White" 
                                                        BorderBrush="#DDD" 
                                                        BorderThickness="1"
                                                        CornerRadius="8"
                                                        Padding="10"
                                                        Margin="0,5">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <StackPanel Grid.Column="0">
                                                            <TextBlock Text="{Binding Name}" 
                                                                       FontWeight="SemiBold"
                                                                       Foreground="#333"/>
                                                            <TextBlock Text="{Binding Brand}" 
                                                                       FontSize="11"
                                                                       Foreground="#666"/>
                                                        </StackPanel>
                                                        <TextBlock Grid.Column="1" 
                                                                   Text="{Binding Price, StringFormat='${0:F2}'}"
                                                                   FontWeight="Bold"
                                                                   Foreground="#0078D4"
                                                                   VerticalAlignment="Center"/>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>

                            <!-- Error Message -->
                            <Border Style="{StaticResource ErrorMessageStyle}"
                                    Visibility="{Binding IsError, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <TextBlock Text="{Binding Content}" 
                                           Foreground="#C62828"
                                           TextWrapping="Wrap"/>
                            </Border>

                            <!-- Thinking Indicator -->
                            <Border Style="{StaticResource ThinkingMessageStyle}"
                                    Visibility="{Binding IsThinking, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="🤔 " Margin="0,0,5,0"/>
                                    <TextBlock Text="Thinking..." 
                                               Foreground="#1976D2"
                                               FontStyle="Italic"/>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Suggested Questions -->
        <Border Grid.Row="3" 
                Background="#FAFAFA" 
                BorderBrush="#E0E0E0"
                BorderThickness="0,1,0,0"
                Padding="10"
                Visibility="{Binding IsProcessing, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
            <StackPanel>
                <TextBlock Text="Suggested Questions:" 
                           FontSize="11" 
                           Foreground="#666"
                           Margin="5,0,0,5"/>
                <WrapPanel>
                    <ItemsControl ItemsSource="{Binding SuggestedQuestions}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Content="{Binding}" 
                                        Style="{StaticResource SuggestionButtonStyle}"
                                        Command="{Binding DataContext.UseSuggestionCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </WrapPanel>
            </StackPanel>
        </Border>

        <!-- Input Area -->
        <Border Grid.Row="4" 
                Background="White" 
                BorderBrush="#E0E0E0"
                BorderThickness="0,1,0,0"
                Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0"
                         x:Name="MessageTextBox"
                         Text="{Binding UserInput, UpdateSourceTrigger=PropertyChanged}"
                         FontSize="14"
                         Padding="12,8"
                         VerticalContentAlignment="Center"
                         BorderBrush="#DDD"
                         BorderThickness="1"
                         Background="#F5F5F5">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="TextBox">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="20">
                                            <ScrollViewer x:Name="PART_ContentHost" 
                                                          Margin="{TemplateBinding Padding}"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsFocused" Value="True">
                                    <Setter Property="BorderBrush" Value="#0078D4"/>
                                    <Setter Property="Background" Value="White"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <Button Grid.Column="1"
                        Content="🗑️"
                        FontSize="16"
                        Width="40"
                        Height="40"
                        Margin="10,0"
                        AutomationProperties.Name="Clear chat"
                        Background="Transparent"
                        BorderThickness="0"
                        ToolTip="Clear chat"
                        Command="{Binding ClearChatCommand}"/>

                <Button Grid.Column="2"
                        Content="➤"
                        FontSize="16"
                        Width="45"
                        Height="45"
                        AutomationProperties.Name="Send message"
                        Background="#0078D4"
                        Foreground="White"
                        BorderThickness="0"
                        ToolTip="Send message"
                        Command="{Binding SendMessageCommand}">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="22.5">
                                            <ContentPresenter HorizontalAlignment="Center" 
                                                              VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#005A9E"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Background" Value="#CCC"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Border>
    </Grid>
</Window>
