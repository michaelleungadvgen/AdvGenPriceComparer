<ui:FluentWindow x:Class="AdvGenPriceComparer.WPF.Views.PriceDropNotificationsWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Title="Price Drop Notifications" 
                 Height="700" Width="900"
                 WindowStartupLocation="CenterOwner"
                 Background="White">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="White" Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock Text="📢 Price Drop Notifications" FontSize="24" FontWeight="Bold"/>
                    <TextBlock Text="Get notified when prices drop on your watched items" 
                               FontSize="12" Opacity="0.7" Margin="0,5,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <ui:Badge Appearance="Danger" 
                              Content="{Binding UnreadCount}"
                              Visibility="{Binding UnreadCount, Converter={StaticResource CountToVisibilityConverter}}"
                              Margin="0,0,10,0"/>
                    <ui:ToggleSwitch Content="Monitoring" 
                                     IsChecked="{Binding IsMonitoring}"
                                     Command="{Binding ToggleMonitoringCommand}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- New Alert Section -->
        <Border Grid.Row="1" Background="#F5F5F5" Padding="20,15" BorderBrush="#E0E0E0" BorderThickness="0,1">
            <Expander Header="Create New Price Drop Alert">
                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Labels -->
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Item ID *" FontWeight="SemiBold" Margin="0,0,10,5"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="Threshold %" FontWeight="SemiBold" Margin="0,0,10,5"/>
                    <TextBlock Grid.Row="0" Grid.Column="2" Text="Alert Name" FontWeight="SemiBold" Margin="0,0,10,5"/>

                    <!-- Inputs -->
                    <TextBox Grid.Row="1" Grid.Column="0" 
                             Text="{Binding NewAlertItemId}" 
                             Margin="0,0,10,0"
                             VerticalAlignment="Center"/>
                    <TextBox Grid.Row="1" Grid.Column="1" 
                             Text="{Binding NewAlertThresholdPercentage}" 
                             Margin="0,0,10,0"
                             VerticalAlignment="Center"/>
                    <TextBox Grid.Row="1" Grid.Column="2" 
                             Text="{Binding NewAlertName}" 
                             Margin="0,0,10,0"
                             VerticalAlignment="Center"/>
                    <Button Grid.Row="1" Grid.Column="3" 
                            Content="Create Alert" 
                            Command="{Binding CreateAlertCommand}"
                            Padding="20,5"
                            VerticalAlignment="Center"/>
                </Grid>
            </Expander>
        </Border>

        <!-- Notifications List -->
        <Grid Grid.Row="2" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Toolbar -->
            <ToolBarTray Grid.Row="0" Margin="0,0,0,10">
                <ToolBar>
                    <Button Content="🔄 Refresh" Command="{Binding RefreshCommand}" Padding="10,5"/>
                    <Separator/>
                    <Button Content="✓ Mark All Read" Command="{Binding MarkAsReadCommand}" Padding="10,5"/>
                    <Button Content="🗑 Clear All" Command="{Binding ClearAllCommand}" Padding="10,5"/>
                </ToolBar>
            </ToolBarTray>

            <!-- Notifications DataGrid -->
            <DataGrid Grid.Row="1" 
                      ItemsSource="{Binding Notifications}"
                      SelectedItem="{Binding SelectedNotification}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      GridLinesVisibility="Horizontal">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Alert Name" Binding="{Binding AlertName}" Width="200"/>
                    <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*"/>
                    <DataGridTextColumn Header="Previous" Binding="{Binding PreviousPrice, StringFormat='${0:F2}'}" Width="80"/>
                    <DataGridTextColumn Header="Current" Binding="{Binding CurrentPrice, StringFormat='${0:F2}'}" Width="80"/>
                    <DataGridTextColumn Header="Triggered" Binding="{Binding LastTriggered, StringFormat='{}{0:g}'}" Width="120"/>
                    <DataGridCheckBoxColumn Header="Read" Binding="{Binding IsRead}" Width="60"/>
                    <DataGridTemplateColumn Header="Actions" Width="150">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Read" 
                                            Command="{Binding DataContext.MarkAsReadCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}"
                                            Padding="8,2" Margin="2"
                                            Visibility="{Binding IsRead, Converter={StaticResource InverseBooleanToVisibilityConverter}}"/>
                                    <Button Content="Dismiss" 
                                            Command="{Binding DataContext.DismissCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}"
                                            Padding="8,2" Margin="2"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Empty State -->
            <TextBlock Grid.Row="1" 
                       Text="No price drop notifications yet.&#x0a;Create an alert to get notified when prices drop!"
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center"
                       FontSize="16"
                       Opacity="0.5"
                       TextAlignment="Center"
                       Visibility="{Binding Notifications.Count, Converter={StaticResource ZeroToVisibilityConverter}}"/>
        </Grid>
    </Grid>
</ui:FluentWindow>
