<Window x:Class="AdvGenPriceComparer.WPF.Views.ImportDataWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Import JSON Data"
        Height="600"
        Width="700"
        WindowStartupLocation="CenterScreen"
        Topmost="True"
        ShowInTaskbar="True"
        ResizeMode="NoResize"
        Loaded="Window_Loaded">
    <Window.Resources>
        <Style TargetType="TextBlock" x:Key="LabelStyle">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,10,0,5"/>
        </Style>
        <Style TargetType="TextBox" x:Key="InputStyle">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderBrush" Value="#ddd"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style TargetType="ComboBox" x:Key="ComboStyle">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderBrush" Value="#ddd"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Step 1: File Selection and Store/Date -->
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" Visibility="{Binding Step1Visibility}">
            <StackPanel Margin="40,30,40,30">
                <TextBlock Text="Step 1: Select Files and Details" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,20"/>

                <!-- File Selection -->
                <TextBlock Text="JSON Files *" Style="{StaticResource LabelStyle}"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox Grid.Column="0"
                             Style="{StaticResource InputStyle}"
                             Text="{Binding SelectedFilesText, Mode=OneWay}"
                             IsReadOnly="True"
                             Background="#f9f9f9"/>
                    <Button Grid.Column="1"
                            Content="Browse..."
                            Width="100"
                            Height="36"
                            Margin="10,0,0,0"
                            Click="BrowseFiles_Click"
                            Background="#f5f5f5"
                            Foreground="#333"
                            BorderBrush="#ddd"
                            BorderThickness="1"
                            Cursor="Hand"
                            FontSize="13"/>
                </Grid>

                <!-- Store Selection -->
                <TextBlock Text="Store *" Style="{StaticResource LabelStyle}" Margin="0,20,0,5"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <ComboBox Grid.Column="0"
                              x:Name="StoreComboBox"
                              Style="{StaticResource ComboStyle}"
                              ItemsSource="{Binding Stores}"
                              SelectedItem="{Binding SelectedStore, UpdateSourceTrigger=PropertyChanged}"
                              DisplayMemberPath="Name"/>
                    <Button Grid.Column="1"
                            Content="New Store"
                            Width="100"
                            Height="36"
                            Margin="10,0,0,0"
                            Click="NewStore_Click"
                            Background="#f5f5f5"
                            Foreground="#333"
                            BorderBrush="#ddd"
                            BorderThickness="1"
                            Cursor="Hand"
                            FontSize="13"/>
                </Grid>

                <!-- Date Selection -->
                <TextBlock Text="Catalogue Date *" Style="{StaticResource LabelStyle}" Margin="0,20,0,5"/>
                <DatePicker x:Name="CatalogueDatePicker"
                            SelectedDate="{Binding CatalogueDate, UpdateSourceTrigger=PropertyChanged}"
                            FontSize="13"
                            Padding="8"
                            BorderBrush="#ddd"
                            BorderThickness="1"/>

                <!-- Expiry Date Selection (Optional) -->
                <TextBlock Text="Discount Expiry Date" Style="{StaticResource LabelStyle}" Margin="0,20,0,5"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <DatePicker x:Name="ExpiryDatePicker"
                                Grid.Column="0"
                                SelectedDate="{Binding ExpiryDate, UpdateSourceTrigger=PropertyChanged}"
                                FontSize="13"
                                Padding="8"
                                BorderBrush="#ddd"
                                BorderThickness="1"/>
                    <Button Grid.Column="1"
                            Content="Same as Record"
                            Width="110"
                            Height="36"
                            Margin="10,0,0,0"
                            Click="SameAsRecordDate_Click"
                            Background="#f5f5f5"
                            Foreground="#333"
                            BorderBrush="#ddd"
                            BorderThickness="1"
                            Cursor="Hand"
                            FontSize="12"/>
                </Grid>
                <TextBlock Text="Optional: Set when the discount expires. Defaults to 7 days after catalogue date."
                           FontSize="11"
                           Foreground="#666"
                           Margin="0,5,0,0"/>

                <!-- File Preview -->
                <TextBlock Text="Selected Files" Style="{StaticResource LabelStyle}" Margin="0,20,0,5"/>
                <Border BorderBrush="#ddd" BorderThickness="1" Background="#f9f9f9" Padding="12" CornerRadius="4">
                    <ScrollViewer MaxHeight="150" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding SelectedFiles}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" FontSize="12" Margin="0,2"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Step 2: Preview Records -->
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" Visibility="{Binding Step2Visibility}">
            <StackPanel Margin="40,30,40,30">
                <TextBlock Text="Step 2: Review Import Records" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>
                <TextBlock Text="Review the items below. Check the box to add a price record to an existing item, or uncheck to create a new item."
                           FontSize="12"
                           Foreground="#666"
                           TextWrapping="Wrap"
                           Margin="0,0,0,20"/>

                <!-- Import Count Summary -->
                <Border BorderBrush="#ddd" BorderThickness="1" Background="#f0f9ff" Padding="12" CornerRadius="4" Margin="0,0,0,15">
                    <TextBlock FontSize="13">
                        <Run Text="Total Records: " FontWeight="SemiBold"/>
                        <Run Text="{Binding PreviewItems.Count, Mode=OneWay}"/>
                    </TextBlock>
                </Border>

                <!-- Loading indicator -->
                <ProgressBar Height="6"
                             Margin="0,0,0,10"
                             IsIndeterminate="True"
                             Visibility="{Binding ProgressVisibility}"/>

                <!-- Preview Items List -->
                <DataGrid ItemsSource="{Binding PreviewItems}"
                          AutoGenerateColumns="False"
                          IsReadOnly="False"
                          SelectionMode="Single"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          BorderThickness="1"
                          BorderBrush="#ddd"
                          Background="White"
                          RowHeight="60"
                          MaxHeight="400"
                          CanUserResizeRows="False"
                          CanUserReorderColumns="False"
                          FontSize="12">

                    <!-- Column Headers Style -->
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#f9f9f9"/>
                            <Setter Property="Foreground" Value="#666"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="FontSize" Value="12"/>
                            <Setter Property="Padding" Value="12,8"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="#ddd"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>

                    <!-- Row Style -->
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="White"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#f9f9f9"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>

                    <DataGrid.Columns>
                        <!-- Checkbox Column -->
                        <DataGridTemplateColumn Header="Action" Width="140">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Vertical" Margin="8,4">
                                        <CheckBox IsChecked="{Binding AddPriceRecordOnly, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                                  IsEnabled="{Binding HasExistingItem}"
                                                  Content="Add price record"
                                                  FontSize="11"/>
                                        <TextBlock Text="{Binding ActionDescription}"
                                                   FontSize="10"
                                                   Foreground="#666"
                                                   Margin="0,2,0,0"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Product Name Column -->
                        <DataGridTemplateColumn Header="Product" Width="2*" MinWidth="200">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="8,4">
                                        <TextBlock Text="{Binding ProductName}" FontWeight="SemiBold" TextWrapping="Wrap"/>
                                        <TextBlock Text="{Binding ExistingItemInfo}" FontSize="10" Foreground="#0066cc" Margin="0,2,0,0"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Brand Column -->
                        <DataGridTextColumn Header="Brand" Binding="{Binding Brand}" Width="*" MinWidth="100">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Margin" Value="8,0"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Category Column -->
                        <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="*" MinWidth="100">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Margin" Value="8,0"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Price Column -->
                        <DataGridTextColumn Header="Price" Binding="{Binding PriceDisplay}" Width="120">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Margin" Value="8,0"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </ScrollViewer>

        <!-- Step 3: Review and Import -->
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" Visibility="{Binding Step3Visibility}">
            <StackPanel Margin="40,30,40,30">
                <TextBlock Text="Step 3: Import Progress" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,20"/>

                <!-- Summary -->
                <Border BorderBrush="#ddd" BorderThickness="1" Background="#f0f9ff" Padding="16" CornerRadius="4" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock FontSize="14" FontWeight="SemiBold" Margin="0,0,0,10">Import Summary</TextBlock>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Files:" FontWeight="SemiBold"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding FileCount}"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Store:" FontWeight="SemiBold" Margin="0,5,0,0"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding SelectedStore.Name}" Margin="0,5,0,0"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Date:" FontWeight="SemiBold" Margin="0,5,0,0"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding CatalogueDate, StringFormat='dd/MM/yyyy'}" Margin="0,5,0,0"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Import Status -->
                <TextBlock Text="Import Status" Style="{StaticResource LabelStyle}"/>
                <Border BorderBrush="#ddd" BorderThickness="1" Background="#f9f9f9" Padding="12" CornerRadius="4" MinHeight="200">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <TextBlock x:Name="ImportStatusTextBlock"
                                   Text="{Binding ImportStatus}"
                                   FontSize="12"
                                   FontFamily="Consolas"
                                   TextWrapping="Wrap"/>
                    </ScrollViewer>
                </Border>

                <!-- Progress Info -->
                <Grid Margin="0,10,0,5" Visibility="{Binding ProgressVisibility}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" 
                               Text="{Binding CurrentItem}" 
                               FontSize="12" 
                               Foreground="#333"
                               TextTrimming="CharacterEllipsis"/>
                    <TextBlock Grid.Column="1" 
                               Text="{Binding ProgressPercent, StringFormat='{}{0}%'}" 
                               FontSize="12" 
                               FontWeight="SemiBold"
                               Foreground="#0066cc"
                               Margin="10,0,0,0"/>
                </Grid>

                <!-- Progress Bar -->
                <ProgressBar Height="8"
                             Margin="0,0,0,0"
                             Minimum="0"
                             Maximum="100"
                             Value="{Binding ProgressPercent}"
                             Visibility="{Binding ProgressVisibility}"/>
            </StackPanel>
        </ScrollViewer>

        <!-- Buttons -->
        <Border Grid.Row="1"
                Background="White"
                BorderBrush="#e8e8e8"
                BorderThickness="0,1,0,0"
                Padding="40,20">
            <Grid>
                <!-- Step 1 Buttons -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Visibility="{Binding Step1Visibility}">
                    <Button Content="Cancel"
                            Width="90"
                            Height="36"
                            Margin="0,0,12,0"
                            Click="Cancel_Click"
                            Background="Transparent"
                            Foreground="#666"
                            BorderThickness="0"
                            Cursor="Hand"
                            FontSize="14"/>
                    <Button Content="Next"
                            Width="90"
                            Height="36"
                            Click="Next_Click"
                            Background="#111"
                            Foreground="White"
                            BorderThickness="0"
                            Cursor="Hand"
                            FontSize="14">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="2"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center"
                                                                VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#333"/>
                                    </Trigger>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Background" Value="#ccc"/>
                                        <Setter Property="Foreground" Value="#888"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>

                <!-- Step 2 Buttons -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Visibility="{Binding Step2Visibility}">
                    <Button Content="Back"
                            Width="90"
                            Height="36"
                            Margin="0,0,12,0"
                            Click="Back_Click"
                            Background="Transparent"
                            Foreground="#666"
                            BorderThickness="0"
                            Cursor="Hand"
                            FontSize="14"
                            IsEnabled="{Binding CanGoBack}"/>
                    <Button Content="Next"
                            Width="90"
                            Height="36"
                            Click="NextToStep3_Click"
                            Background="#111"
                            Foreground="White"
                            BorderThickness="0"
                            Cursor="Hand"
                            FontSize="14">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="2"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center"
                                                                VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#333"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>

                <!-- Step 3 Buttons -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Visibility="{Binding Step3Visibility}">
                    <Button Content="Back"
                            Width="90"
                            Height="36"
                            Margin="0,0,12,0"
                            Click="BackToStep2_Click"
                            Background="Transparent"
                            Foreground="#666"
                            BorderThickness="0"
                            Cursor="Hand"
                            FontSize="14"
                            IsEnabled="{Binding CanGoBack}"/>
                    <Button Content="{Binding ImportButtonText}"
                            Width="90"
                            Height="36"
                            Click="Import_Click"
                            Background="#111"
                            Foreground="White"
                            BorderThickness="0"
                            Cursor="Hand"
                            FontSize="14"
                            IsEnabled="{Binding CanImport}">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="2"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center"
                                                                VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#333"/>
                                    </Trigger>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Background" Value="#ccc"/>
                                        <Setter Property="Foreground" Value="#888"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
