<Page x:Class="AdvGenPriceComparer.WPF.Views.PriceHistoryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Title="Price History"
      Background="White">

    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" BorderBrush="#e8e8e8" BorderThickness="0,0,0,1" Padding="24,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Price History" FontSize="24" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding RecordCountText}" FontSize="13" Foreground="#666" Margin="0,4,0,0"/>
                </StackPanel>

                <Button Grid.Column="1"
                        Content="Add Price Record"
                        Command="{Binding AddPriceRecordCommand}"
                        Width="130"
                        Height="36"
                        Background="#111"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        FontSize="14"
                        VerticalAlignment="Center">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="2"
                                                Padding="12,0">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#333"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Border>

        <!-- Filters -->
        <Border Grid.Row="1" Background="#f9f9f9" BorderBrush="#e8e8e8" BorderThickness="0,0,0,1" Padding="24,16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Item Filter -->
                    <ComboBox Grid.Column="0"
                              Margin="0,0,12,0"
                              Padding="10,8"
                              FontSize="13"
                              BorderBrush="#ddd"
                              ItemsSource="{Binding Items}"
                              SelectedItem="{Binding SelectedItem, UpdateSourceTrigger=PropertyChanged}"
                              DisplayMemberPath="Name"/>

                    <!-- Place Filter -->
                    <ComboBox Grid.Column="1"
                              Margin="0,0,12,0"
                              Padding="10,8"
                              FontSize="13"
                              BorderBrush="#ddd"
                              ItemsSource="{Binding Places}"
                              SelectedItem="{Binding SelectedPlace, UpdateSourceTrigger=PropertyChanged}"
                              DisplayMemberPath="Name"/>

                    <!-- Date Range -->
                    <DatePicker Grid.Column="2"
                                Margin="0,0,8,0"
                                Padding="10,8"
                                FontSize="13"
                                BorderBrush="#ddd"
                                SelectedDate="{Binding FromDate}"
                                ToolTip="From Date"/>
                    
                    <TextBlock Grid.Column="3" Text="to" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="#666"/>
                    
                    <DatePicker Grid.Column="4"
                                Padding="10,8"
                                FontSize="13"
                                BorderBrush="#ddd"
                                SelectedDate="{Binding ToDate}"
                                ToolTip="To Date"/>
                </Grid>
                
                <Grid Grid.Row="1" Margin="0,12,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Search Box -->
                    <Border Grid.Column="0" Background="White" BorderBrush="#ddd" BorderThickness="1" CornerRadius="4" Padding="10,8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="🔍" FontSize="14" Margin="0,0,8,0" Opacity="0.5"/>
                            <TextBox Grid.Column="1"
                                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                     BorderThickness="0"
                                     Background="Transparent"
                                     VerticalAlignment="Center"
                                     FontSize="13"
                                     ToolTip="Search by item, store, or sale description"/>
                        </Grid>
                    </Border>

                    <!-- Show Only Sales Checkbox -->
                    <CheckBox Grid.Column="1"
                              Content="Show Only Sales"
                              IsChecked="{Binding ShowOnlySales}"
                              Margin="16,0,16,0"
                              VerticalAlignment="Center"/>

                    <!-- Clear Filters Button -->
                    <Button Grid.Column="2"
                            Content="Clear Filters"
                            Command="{Binding ClearFiltersCommand}"
                            Width="100"
                            Height="36"
                            Margin="0,0,12,0"
                            Background="Transparent"
                            Foreground="#666"
                            BorderBrush="#ddd"
                            BorderThickness="1"
                            Cursor="Hand"
                            FontSize="13"/>

                    <!-- Refresh Button -->
                    <Button Grid.Column="3"
                            Content="🔄 Refresh"
                            Command="{Binding RefreshCommand}"
                            Width="90"
                            Height="36"
                            Background="Transparent"
                            Foreground="#666"
                            BorderThickness="0"
                            Cursor="Hand"
                            FontSize="13"/>
                </Grid>
            </Grid>
        </Border>

        <!-- Price Records List -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding PriceRecords}"
                  SelectedItem="{Binding SelectedPriceRecord}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  GridLinesVisibility="None"
                  HeadersVisibility="Column"
                  BorderThickness="0"
                  Background="White"
                  RowHeight="48"
                  CanUserResizeRows="False"
                  CanUserReorderColumns="False"
                  FontSize="13">

            <!-- Column Headers Style -->
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="White"/>
                    <Setter Property="Foreground" Value="#666"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="FontSize" Value="12"/>
                    <Setter Property="Padding" Value="24,12"/>
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    <Setter Property="BorderBrush" Value="#e8e8e8"/>
                    <Setter Property="HorizontalContentAlignment" Value="Left"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <!-- Row Style -->
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="White"/>
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    <Setter Property="BorderBrush" Value="#f0f0f0"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#f9f9f9"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#f0f9ff"/>
                            <Setter Property="Foreground" Value="#111"/>
                        </Trigger>
                        <DataTrigger Binding="{Binding IsOnSale}" Value="True">
                            <Setter Property="Background" Value="#f0fff4"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>

            <!-- Cell Style -->
            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Padding" Value="24,0"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Border Background="{TemplateBinding Background}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="#111"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.CellStyle>

            <DataGrid.Columns>
                <!-- Item Column -->
                <DataGridTextColumn Header="Item" Binding="{Binding ItemName}" Width="2*" MinWidth="200"/>

                <!-- Store Column -->
                <DataGridTextColumn Header="Store" Binding="{Binding PlaceName}" Width="*" MinWidth="120"/>

                <!-- Price Column -->
                <DataGridTextColumn Header="Price" Binding="{Binding PriceDisplay}" Width="150">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontWeight" Value="SemiBold"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <!-- Status Column -->
                <DataGridTextColumn Header="Status" Binding="{Binding StatusDisplay}" Width="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsOnSale}" Value="True">
                                    <Setter Property="Foreground" Value="#16a34a"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <!-- Sale Description Column -->
                <DataGridTextColumn Header="Sale Info" Binding="{Binding SaleDescription}" Width="*" MinWidth="150">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#16a34a"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <!-- Date Column -->
                <DataGridTextColumn Header="Date" Binding="{Binding DateRecorded, StringFormat='dd/MM/yyyy'}" Width="100"/>

                <!-- Expiry Column -->
                <DataGridTextColumn Header="Expires" Binding="{Binding ValidTo, StringFormat='dd/MM/yyyy'}" Width="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ValidTo}" Value="{x:Null}">
                                    <Setter Property="Text" Value="-"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <!-- Source Column -->
                <DataGridTextColumn Header="Source" Binding="{Binding Source}" Width="100"/>

                <!-- Actions Column -->
                <DataGridTemplateColumn Header="Actions" Width="180">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button Content="Edit"
                                        Command="{Binding DataContext.EditPriceRecordCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                        CommandParameter="{Binding}"
                                        Background="Transparent"
                                        Foreground="#555"
                                        BorderThickness="0"
                                        Padding="12,6"
                                        Cursor="Hand"
                                        FontSize="13"
                                        Margin="0,0,8,0"/>
                                <Button Content="Delete"
                                        Command="{Binding DataContext.DeletePriceRecordCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                        CommandParameter="{Binding}"
                                        Background="Transparent"
                                        Foreground="#dc2626"
                                        BorderThickness="0"
                                        Padding="12,6"
                                        Cursor="Hand"
                                        FontSize="13"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
